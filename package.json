{
  "name": "gmail-ai-assistant",
  "version": "1.0.32",
  "description": "Chrome Extension — Gmail AI Assistant (Manifest V3)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node --test",
    "lint": "eslint \"src/**/*.js\" \"bot/**/*.js\" \"test/**/*.js\" build.js",
    "lint:fix": "eslint \"src/**/*.js\" \"bot/**/*.js\" \"test/**/*.js\" build.js --fix"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "eslint": "^9.0.0"
  },
  "dependencies": {
    "@zxing/library": "^0.23.0",
    "firebase": "^12.18.0"
  }
}
