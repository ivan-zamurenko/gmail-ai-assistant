{
  "name": "gmail-ai-assistant",
  "version": "1.0.0",
  "description": "Chrome Extension — Gmail AI Assistant (Manifest V3)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "lint": "eslint \"src/**/*.js\"",
    "lint:fix": "eslint \"src/**/*.js\" --fix"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "eslint": "^9.0.0"
  }
}
